<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Gerenciamento de Categorias</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-900 min-h-screen">
    <!-- Barra de menu horizontal -->
    <nav class="bg-gray-800 shadow-lg w-full">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
            <div class="flex justify-between h-16">
                <div class="flex space-x-8">
                    <a href="/admin/usuarios" class="text-gray-300 hover:bg-gray-700 hover:text-white px-3 py-2 rounded-md text-sm font-medium transition duration-200">Usuários</a>
                    <a href="/admin/categorias" class="text-gray-300 hover:bg-gray-700 hover:text-white px-3 py-2 rounded-md text-sm font-medium transition duration-200">Categorias</a>
                    <a href="/admin/videos" class="text-gray-300 hover:bg-gray-700 hover:text-white px-3 py-2 rounded-md text-sm font-medium transition duration-200">Vídeos</a>
                    <a href="/admin/config" class="text-gray-300 hover:bg-gray-700 hover:text-white px-3 py-2 rounded-md text-sm font-medium transition duration-200">Configuração</a>
                    <a href="/admin/sair" class="text-gray-300 hover:bg-gray-700 hover:text-white px-3 py-2 rounded-md text-sm font-medium transition duration-200">Sair</a>
                </div>
                <div class="flex items-center">
                    <span class="text-gray-300 text-sm font-medium"><%= admNome %></span>
                </div>
            </div>
        </div>
    </nav>

    <!-- Conteúdo principal -->
    <main class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
        <div class="bg-gray-800 p-6 rounded-lg shadow-lg">
            <div class="flex justify-between items-center mb-4">
                <h2 class="text-xl font-bold text-white">Categorias</h2>
                <a href="/admin/novacategoria" class="bg-blue-600 hover:bg-blue-700 text-white font-semibold py-2 px-4 rounded-md transition duration-200">Incluir Categoria</a>
            </div>

            <!-- Mensagem de Sucesso/Erro -->
            <% if (mensagem) { %>
                <div class="mb-4 p-3 rounded-md <%= sucesso ? 'bg-green-600' : 'bg-red-600' %> text-white">
                    <%= mensagem %>
                </div>
            <% } %>

            <div class="overflow-x-auto">
                <table class="w-full text-left text-gray-300">
                    <thead>
                        <tr class="bg-gray-700">
                            <th class="px-4 py-2">Nome</th>
                            <th class="px-4 py-2">Nome Normalizado</th>
                            <!-- <th class="px-4 py-2">Quantidade de Filmes</th> -->
                            <th class="px-4 py-2 text-right">Ações</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% categorias.forEach(categoria => { %>
                            <tr class="border-b border-gray-700 hover:bg-gray-700">
                                <td class="px-4 py-2"><%= categoria.catnome %></td>
                                <td class="px-4 py-2"><%= categoria.catnomenormal %></td>
                                <!-- <td class="px-4 py-2"><%= categoria.quantidade %></td> -->
                                <td class="px-4 py-2 text-right">
                                    <a href="/admin/atualizarcategoria/<%= categoria.catcodigo %>" class="text-blue-400 hover:text-blue-300 mr-2">Atualizar</a>
                                    <a href="/admin/excluircategoria/<%= categoria.catcodigo %>" class="text-red-400 hover:text-red-300">Excluir</a>
                                </td>
                            </tr>
                        <% }) %>
                    </tbody>
                </table>
            </div>
        </div>
    </main>
</body>
</html>